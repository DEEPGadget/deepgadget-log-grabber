# Manycore 진단 로그 수집 스크립트 (단일 Bash) - 작업 규칙

## 0) 범위
- 이 저장소는 **단일 Bash 스크립트**(`./<script_name>.sh`)를 유지/개선/확장한다.
- 사용자가 명시적으로 요청하지 않는 한 **파일을 분리하거나 다중 스크립트로 쪼개지 않는다.**

## 1) 스타일/톤 (반드시 기존 코드와 동일하게)
- 현재 스크립트의 **절차형 흐름 + 일부 함수** 스타일을 유지한다.
- 주석과 사용자 출력 메시지는 **현행 스크립트처럼 영어 유지**(기존과 일관성 유지 목적).
- “대규모 리팩터링”을 금지한다.
  - 예: 전체 구조를 바꾸는 프레임워크화, 과도한 함수 분해, 파일 분리 등
- 작은 단위로 개선하고, 기존 실행 흐름/디렉터리 구조/결과물 포맷을 보존한다.

## 2) 실행/상호작용 방식 (유지)
- 사용자는 보통 다음처럼 실행한다: `./<script_name>.sh`
- 스크립트는 `/dev/tty`를 통해 **도구 설치 여부를 인터랙티브로 물어볼 수 있어야 한다.**
- 비대화형 실행을 위해 환경변수를 유지한다:
  - `SKIP_TOOLS=0` : 설치 프롬프트에 자동으로 Y로 응답(설치 허용)
  - `SKIP_TOOLS=1` : 패키지 설치 절대 금지
- 사용자가 요청하지 않는 한, **현재의 프롬프트/환경변수 동작을 제거하거나 변경하지 않는다.**

## 3) 권한/안전 가드레일
- 스크립트는 필요한 경우 `sudo`를 사용해 읽기 위주의 정보를 수집한다.
- 시스템 상태를 바꾸는 동작은 원칙적으로 금지하며, 예외는 다음만 허용:
  - 임시 디렉터리 생성/삭제
  - 로그 파일 복사
  - (옵션) 패키지 설치
  - tar.gz 아카이브 생성
- 그 외 “시스템 상태 변경”이 필요한 기능을 추가할 땐 반드시:
  - 옵션/환경변수로 **명시적 동의**를 받거나,
  - 문서(이 파일) 및 스크립트 안내 메시지에 **명확히 고지**한다.

## 4) 의존성/패키지 관리 정책 (현재 방식 유지)
- 현재는 Ubuntu/Debian 기반(`apt-get`)을 전제로 한다.
- 새로운 외부 도구를 추가해야 한다면:
  - 반드시 `NEEDED_TOOLS` 배열에 아래 4필드 규칙으로 추가한다:
    1) 실행 파일명(체크 대상)
    2) apt 패키지명
    3) 체크 상태(0/1/2)
    4) VM에서 유용 여부(0/1)
  - 설치/체크 흐름은 기존 로직을 따른다:
    - `command -v`로 존재 확인
    - `SKIP_TOOLS` 존중
    - VM인 경우(`IS_VIRTUAL_MACHINE=1`) VM 비유용 툴은 설치/수집 생략

## 5) 스크립트 상위 흐름 (대체로 유지)
다음 상위 순서를 가능한 한 유지한다:
1) 안내/디스클레이머 출력
2) 가상화 여부 판별
3) tools 배열 검증
4) 도구 체크/설치(옵션)
5) 임시 디렉터리 및 결과 디렉터리 트리 생성
6) 카테고리별 정보 수집(로그/GPU/스토리지/네트워크 등)
7) 아카이브(tar.gz) 생성
8) 임시 디렉터리 정리
9) 완료 메시지 출력

## 6) 결과물 디렉터리 구조 (보존)
- `Manycore-bug-report/` 하위 디렉터리명을 변경하지 않는다:
  - `drives-and-storage/`
  - `system-logs/`
  - `repos-and-packages/`
  - `networking/`
  - `gpu-memory-errors/`
  - `bmc-info/`
  - `grub/`
- 새 카테고리가 필요하면 `Manycore-bug-report/` 아래에 **새 서브디렉터리**를 추가하되, 기존 이름은 유지한다.

## 7) 에러 처리/수집 철학 (현행: best-effort 유지)
- 기본 원칙은 **최대한 많이 수집하고, 일부 실패해도 전체 실행은 계속**한다.
- 특정 수집기가 실패하거나 출력이 비어 있으면:
  - 해당 파일에 “No <X> data available …” 형태의 **설명 텍스트를 남긴다.**
- 선택 수집 기능 실패 때문에 스크립트 전체가 중단되게 만들지 않는다(사용자가 명시적으로 요구한 경우 제외).

## 8) 출력/진행 메시지 규칙
- 진행 메시지는 현행처럼 `echo` 기반으로 간결하게 유지한다.
- 상세 로그는 stdout에 과도하게 찍지 않고 **파일로 수집**한다.
- 별도의 로깅 프레임워크/장황한 verbose 시스템은 요청 없이는 도입하지 않는다.

## 9) 민감정보 취급
- 산출물에는 민감정보가 포함될 수 있으므로, 안내 문구(디스클레이머)는 유지/확장한다.
- 다음 범주의 수집은 금지(요청 시에만 별도 논의):
  - 개인 키/인증서 원본(예: `~/.ssh/id_*`)
  - 패스워드/토큰이 그대로 담긴 파일
  - 브라우저/패스워드 매니저 자격증명 저장소
- 새 수집 항목이 민감도를 높이면, 디스클레이머에 명확히 반영한다.

## 10) “금지되는 리팩터링” (강제)
- 사용자가 명시적으로 요청하지 않는 한 아래는 하지 않는다:
  - `set -euo pipefail` 도입을 포함한 “strict mode 대개편”
  - 전체 `getopts` 재작성/CLI 대수술
  - trap 프레임워크 대규모 도입
  - 스크립트 파일 분리/구조 전면 개편
- 단, 버그 수정/경미한 안정성 개선은 허용하되 “현행 스타일”을 깨지 않는다.

## 11) 변경 결과 제출 형식(Claude 응답 규칙)
- 기본은 **unified diff 패치**로 제시한다.
- 변경이 큰 경우에만 전체 스크립트를 제공한다.
- 변경 후에는 아래 3개 수동 검증 커맨드를 항상 제시한다:
  1) 기본 실행
  2) `SKIP_TOOLS=1` 실행
  3) `SKIP_TOOLS=0` 실행(비대화형 설치 허용)
